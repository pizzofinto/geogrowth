import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Dashboard', // Questo è il titolo che sostituirà %s
};

export default function DashboardPage() { return <div>Dashboard Page</div>; }