import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Admin', // Questo è il titolo che sostituirà %s
};
export default function AdminPage() { return <div>Admin Page</div>; }