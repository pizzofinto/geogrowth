import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Components', // Questo è il titolo che sostituirà %s
};

export default function ComponentsPage() { return <div>Components Page</div>; }