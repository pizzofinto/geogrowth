import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Analytics', // Questo è il titolo che sostituirà %s
};
export default function AnalyticsPage() { return <div>Analytics Page</div>; }